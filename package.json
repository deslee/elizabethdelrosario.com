{
  "name": "elizabethdelrosario.com",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "lerna bootstrap && lerna exec --scope admin npm install && lerna run --scope admin build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lerna": "^3.15.0"
  },
  "devDependencies": {}
}
